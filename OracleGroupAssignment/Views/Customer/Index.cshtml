@model IEnumerable<OracleGroupAssignment.Models.Customer>

@{
    ViewData["Title"] = "Index";
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Customer List</h3>
    </div>
    <!-- /.card-header -->
    <div class="card-body">        
<p>
    <a class="btn btn-primary btn-sm" asp-action="Create">Create New Customer</a>
</p>
        <table id="example1" class="table table-bordered table-striped">
    <thead>
        <tr>
            @* <th>
                @Html.DisplayNameFor(model => model.IsHidden)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsDefault)
            </th> *@
            <th>
                @Html.DisplayNameFor(model => model.CustomerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanyName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Phone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            @* <td>
                @Html.DisplayFor(modelItem => item.IsHidden)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsDefault)
            </td> *@
            <td>
                @Html.DisplayFor(modelItem => item.CustomerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                            <a class="btn btn-primary btn-sm" asp-action="Edit" asp-controller="Customer" asp-route-id="@item.CustomerId"><i class=" fas fa-edit"></i> Edit</a>
                            <a class="btn btn-secondary btn-sm" asp-action="Details" asp-controller="Customer" asp-route-id="@item.CustomerId"><i class="fa fa-eye"></i> Details</a>
                            <a class="btn btn-danger btn-sm" asp-action="Delete" asp-controller="Customer" asp-route-id="@item.CustomerId"><i class="fa fa-trash"></i> Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</div>
</div>
